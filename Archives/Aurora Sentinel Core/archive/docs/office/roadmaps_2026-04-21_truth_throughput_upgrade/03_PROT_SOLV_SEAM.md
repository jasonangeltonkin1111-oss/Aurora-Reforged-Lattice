# Protected Solved Seams

## Purpose

This file lists the seams that appear materially healthier now and must be protected while other work proceeds.
These are not “never inspect again” items.
They are “do not reopen blindly” items.

## Protected solved seams

### P1 — Global Top 10 deep batch family

Observed posture:

- run-full / batch drain / verify path appears healthy
- deep children are landing
- verify counters appear healthy

Protection rule:

- do not touch Global Top 10 family identity, queue, retry, or verification logic unless a fresh regression is proven

### P2 — deep selected-symbol root preservation

Observed posture:

- deep selected-symbol/root path appears healthy
- current focus support path appears healthy
- same-symbol deep preservation appears healthy

Protection rule:

- no ordinary publication work may downgrade a lawfully completed deep root/current-focus pair

### P3 — Market Board authority

Observed posture:

- board remains ordinary command authority
- support artifacts appear split-role, not primary authority

Protection rule:

- no dossier/current-focus work may quietly replace board authority

### P4 — manual action family behavior already proven healthy

Observed posture:

- manual run-full / downstream refresh / board refresh behavior appears materially healthier than earlier cycles

Protection rule:

- do not rework action-family semantics from scratch while fixing dossier law or throughput unless a fresh contradiction is proven

### P5 — no second analysis path

Protection rule:

- future passes may not introduce a parallel truth owner, helper chain, or shadow path that duplicates ranking/deep behavior

### P6 — no second owner path

Protection rule:

- future passes may not introduce a second board owner, dossier owner, or selected-symbol owner

## What a future pass must state explicitly before touching these seams

A pass must say:

1. which protected seam is at risk
2. why touching it is unavoidable
3. what exact regression fence will protect it
4. what live test will prove it stayed healthy

If a pass cannot say those four things, it must not touch the protected seam.
