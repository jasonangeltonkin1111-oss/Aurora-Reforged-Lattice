## RUN294R Coding Brain Law — Verdict Context Transport

Runtime verdict fields are transport outputs from the source-owned family plan, not writer-side guesses. Dossier and SDP Lite must consume one compact verdict context for family_id, snapshot_id, refresh_pass, bucket, rank, current-family match, board-snapshot match, main↔lite match, stale/replan, complete-due, and permission=false fields. If a value is unavailable, emit owner/reason/retry state. Never hardcode top-level complete, never let Lite calculate rank or membership, and never use folder/archive/staging presence as current truth.

Failure prevented: Main/Lite split-brain, fake complete verdicts, stale warmup completion, and hidden source-of-truth leaks.

## RUN293R Coding Brain Law — Debloat Before Gate Inflation

When Top5PerBucket publication is slow or partial, repair the work shape before adding blocking layers. Warmup may publish a clearly marked limited first pass, but the next refresh must either complete the same source-owned family snapshot or emit exact owner/reason/retry fields. Bucket coverage comes before deep hydration: seed crypto, energy, fx, indices, metals, and stocks fairly before building rich narrative for early buckets. Dossier and SDP must consume one shared family plan; Lite may differ by content depth only. Complete pass output may not contain ownerless generic unknowns, fake ATR/session/L3/L4 values, Lite-side truth, FileIO/final path mutation, timer lowering, broad scans, or trade/signal/execution/risk permission.

Failure prevented: a faster Market Board hiding slow, starved, placeholder-heavy, or split Main/Lite child truth.

## RUN290R Coding Brain Law — Upload Selection Projects Existing Board Order Only

Market Board upload selection must be a cheap projection from existing source-owned candidate order. It may expose count law, blocker reasons, candidate symbols, review readiness, signature state, data-cleanliness state, and L3/L4 boundary effects. It may not add CopyTicks, CopyRates, CopyBuffer, indicator calls, timer lowering, broad folder scans, FileIO mutation, child rebuild triggers, formula edits, rank/order/membership edits, HUD edits, generated-output edits, or permission changes. Count values are restricted to 0, 3, 5, and 10.

Acceptance constraint: `SIAM_UPLOAD_SELECTOR` is present, review-only, permission-false, signature-aware, data-cleanliness-aware, L3/L4-boundary-aware, and honest about compile/runtime proof being pending.

Failure prevented: hidden compute, retry pressure, stale-folder truth, source-of-truth drift, and upload recommendations masquerading as signals.

## RUN289R Coding Brain Law — L3/L4 Boundary Hydrates or Quarantines

A writer may project L3/L4 metadata only when the committed state is already present as a typed/source-owned object or lawful shortlist projection in that writer path. If the state is absent, the writer must emit debug-quarantined/unavailable fields and must not invent values from FunctionResults prose, folder residue, generated output, or Lite-side calculation. Hydration must not add CopyTicks, CopyRates, CopyBuffer, iATR, iADX, iRSI, timer lowering, broad folder scans, FileIO mutation, child rebuild triggers, formula edits, rank/order/membership edits, HUD edits, or permission changes.

Acceptance constraint: Market Board / SDP bodies expose `L3_L4_BOUNDARY_CONTRACT` and either hydrated source-owned L3/L4 projection or explicit debug quarantine with permissions false.

Failure prevented: hidden compute, Lite split-brain, fake review authority, and source-of-truth leakage across the L3/L4 boundary.

## RUN288R Stable Required-Token Contract Law

Required-token validation is schema, not typography. Hard required-token contracts must prefer stable machine fields; display headings are allowed for readability but must not be schema gates unless explicitly aliased by stable fields. Validation stays active, degraded/unknown publication is allowed when truthful, and no proof surface may fake clean/complete. This law does not grant live proof or trade/signal/execution/risk permission.

## RUN287R Coding Brain Law — Token Parity Is Source-Owned and Cheap

Scanner Status and Artifact Bundle Manifest required-token contracts must remain active and must be checked against their own emitted body fields. New contract fields must be emitted before validation, and parity summaries must report required-tokens-present, missing tokens, and first missing token without filesystem scans, FileIO mutation, timer lowering, retry loops, child rebuilds, formula edits, rank/order/membership edits, HUD edits, or generated-output edits.

Acceptance constraint: static source must show a shared cheap string-search parity helper, Scanner Status parity fields, Manifest parity fields, preserved Market Board fast-lane/signature-barrier blocks, and no forbidden owner changes.

Failure prevented: token-contract drift, report-only proof traps, and validation deletion disguised as repair.

## RUN286R Coding Brain Law — Signature Barrier Gates Clean Board Authority

Market Board fast-lane fields are projection fields only. They must not lower timers, trigger child rebuilds, scan folders broadly, mutate FileIO/final paths, or become authority. Clean authority requires compatible board-visible, Dossier committed, and SDP mirror signatures for GlobalTop10 and Top5 symbol/rank-order truth. If any required signature is unavailable, pending, or mismatched, the source must emit the degraded/blocked state and set recommended dossier request count to zero.

Acceptance constraint: static source must show `BOARD_CHILD_SIGNATURE_BARRIER_BEGIN/END`, `TOP5_SYMBOL_TRUTH_GATE_BEGIN/END`, `count_complete_is_truth_complete=false`, `top5_count_complete_is_symbol_truth=false`, fast-lane-not-authority fields, unchanged formula/rank/order/membership/FileIO/HUD/generated-output owners, and no new market-data calls or timer lowering.

Failure prevented: count parity and fast publication being mistaken for symbol truth or Siam upload authority.

## RUN285R Coding Brain Law — Publication Contract Tokens Must Be Emitted Fields

Required-token arrays for Scanner Status and Artifact Bundle Manifest must reference stable emitted fields, not display-only headings unless the heading is intentionally emitted. Body parity is a source acceptance gate: every new required token must be grep-verifiable in the emitted body path.

Market Board fast lane may add cheap observability fields for dirty state, signatures, latency, due state, and failure isolation. It must not lower the timer as the fix, add broad scans, trigger child Dossier/SDP rebuilds, mutate FileIO/final paths, or change formulas, rank, order, or membership.

Acceptance constraint: static checks must show the fast-lane and sync-latency fences, no new market-data calls, no timer-period lowering, no forbidden owners changed, and request count blocked whenever sync is non-matched.

## RUN283R Coding Brain Law — Active Source Identity Must Outrank Patch-Package Memory

A repair package is not active truth until the compile-visible source tree carries the repair and the current run identity. If Git/source/package/runtime identities disagree, patch or hold before live proof. Identity locks must update the included `ASC_Version.mqh` owner, not only reports or office files. Runtime proof must never be claimed from stale output.

Acceptance constraint: source inspection must identify the included version owner, verify repair owners are present in active source, preserve forbidden owners, and seed the next live proof without editing generated output.

Failure prevented: RUN280/RUN282/RUN283 split-brain, fake proof from stale runtime output, and running live tests against the wrong source tree.

## RUN279R Coding Brain Law — Enum Comparisons Stay Enum-to-Enum

`ASC_SelectionFilterResultState` and equivalent state enums must not be assigned directly to `string` variables or compared through string intermediates. Count logic must compare enum values to enum constants; only output text may use an explicit helper or `EnumToString()`.

Acceptance constraint: static source must show no implicit enum-to-string assignment in the patched Market Board L3 explanation seam, no string-to-enum comparisons, preserved RUN278 Lite fences, unchanged L3/L4 formula/rank/order/membership owners, unchanged FileIO/HUD/generated output owners, and no new market-data collection calls.

Failure prevented: clean compile-with-warnings hiding a type-contract defect in parser-grade L3/L4 explanation output.

## RUN278R Coding Brain Law — Compact Lite Sections Use Existing Owners Only

Lite content-enrichment blocks must be source-owned projections. Do not add new market-data/indicator collection ownership inside enrichment, do not scan folders, do not mutate FileIO/final/archive/cache/staging routes, and do not turn missing facts into clean values. Required-token validation must include the new Lite section fences and false permission fields.

Acceptance constraint: static source must show the seven RUN278R Lite fences, preserved `SIAM_DATA_CLEANLINESS`, preserved L3/L4 explanation fences, unchanged L3/L4 formula/rank/order/membership owners, unchanged FileIO/HUD/generated output owners, and no added direct collection calls for enrichment.

Failure prevented: heartbeat load, split-brain Lite truth, fake ATR/OHLC/tick readiness, and parser drift.

## RUN277R Coding Brain Law — Canonical Child Sync Blocks L4 Overclaim

Market Board L4 helpers must consume canonical `board_child_sync_status` values only: `matched`, `mismatched`, `pending`, `unknown`. Helper logic must treat `blocking` as a boolean outcome, not a status string. Unexpected values canonicalize to `unknown` and must not unlock `fresh` or `clean`.

`l4_members` must not carry CSV symbols. Use numeric `l4_members` / `l4_member_count` and reserve CSV membership for `l4_member_symbols`. Required-token contracts must validate the split fields.

Failure prevented: clean/fresh false positives during Main/Lite child mismatch and downstream parser ambiguity.

## RUN276R Coding Brain Law — Explanation Blocks Are Cheap Projections

L3/L4 explanation fields must be emitted from already-available source-owned structs, signatures, and timestamps. If a writer does not receive committed L3/L4 runtime state, it must emit parser-grade `unknown` placeholders and report the source-owner gap instead of scanning folders, recalculating ranks, or inventing freshness.

Acceptance constraint: `L3_EXPLANATION_BEGIN/END` and `L4_EXPLANATION_BEGIN/END` may be added to Market Board and Lite SDP contracts, but L3/L4 formula owners, rank/order/membership logic, FileIO, archive/cache/staging, HUD, and trade/risk owners remain untouched.

Failure prevented: hidden heartbeat load, stale filesystem truth, Lite split-brain, and compile/runtime regressions from broad rewrites.

## RUN275R Coding Brain Law — Siam Cleanliness Is Projection, Not Ownership

Siam request and data-cleanliness blocks may expose source-owned state only. They must not scan folders from heartbeat, mutate FileIO/final paths, delete/move generated files, compute rank/order/membership, alter L3/L4 formulas, or turn unknowns into clean states.

Readiness must be explicit as `yes`, `limited`, or `no`; unknown data must remain unknown.

Failure prevented: hidden compute, stale filesystem truth, fake clean data, and compile/runtime risk from broad cleanup loops.

## RUN272R Coding Brain Amendment — Parser-Grade SIAM Fence + Empty-Bucket Top5 Reconcile

The SIAM request block must be machine-parseable: exact begin/end fences, source/run/version lineage, source/publication epoch fields, Main/Lite ownership fields, signature fields, and explicit false permissions. Human headings alone are not parser-grade proof.

Top5 Lite empty-bucket repair belongs at the existing family-cycle final-set reconcile boundary. Do not add heartbeat scans, retry loops, FileIO rewrites, final-path mutation, generated-output cleanup, or L3/L4 changes. GlobalTop10 remains conservative; only Top5PerBucket uses source-owned empty-bucket archive/delete policy.

Acceptance constraint: static source must show `SIAM_DOSSIER_REQUEST_BLOCK_BEGIN/END`, full required-token coverage, GlobalTop10 prune flag false, Top5PerBucket prune flag true, and unchanged L3/L4/FileIO/HUD owners.

Failure prevented: visible board confidence and stale Lite children escaping through weak parser fences or folder residue.

## RUN262R Coding Brain Amendment — Timestamp-First Family Snapshot Archive

For Lite Symbol Data Pack archive work, patch only the timestamped-history owner helpers unless direct source inspection proves a different active owner. The lawful shape is writer-produced family snapshot topology: `archive/<family>/<timestamp>/...`, with Top5 bucket folders below the timestamp. The `_archive_manifest.txt` belongs at the family snapshot level.

Do not fix archive topology by deleting, moving, or cleaning generated folders. Do not rewrite FileIO, current/final routes, staging, membership, ranking, HUD, or trade logic. Archive write failure must remain observable and bounded; it must not poison current/final publication.

Acceptance constraint: static source must show timestamp-first Top5 archive path helpers, family-snapshot-level manifest path, unchanged `_previous_state_cache` helpers, unchanged FileIO, and explicit non-current truth labels.

Failure prevented: a cosmetic folder cleanup pretending to be a source repair, or archive history leaking authority into current route truth.

## RUN259R Guidebook Index Amendment — Heartbeat First, Mirror Second

Heartbeat breathing is the first repair gate. Scanner Status, Market Board, FunctionResults, Manifest, heartbeat telemetry, and any cheap open-symbol proof must be able to refresh/degrade before GlobalTop10, Main/Lite parity, archive history, Siam polish, HUD, or strategy work is expanded.

Failure prevented: route/parity/archive patches being stacked onto a timer lane that cannot breathe.

Deep analysis is enrichment only. A pending/deferred Deep Analysis child may label output as degraded route truth, but it may not block canonical family publication indefinitely and may not fake completed deep fields.

Failure prevented: GlobalTop10 or Top5 refresh freezing behind `deep_trigger_pending` while the system keeps logging activity.

Manifest required tokens must be stable emitted contract fields. Active required tokens must not carry stale future-run claims such as old `next_live_run` laws. Legacy run law may remain only as explicitly legacy context, never as an active proof requirement.

Failure prevented: a stale token poisoning every heartbeat and making the Manifest fail forever.

Lite is a mirror/slave contract. Symbol Data Packs must mirror Dossier route family, bucket layout, symbol set, rank/order where relevant, source snapshot, publication epoch, cadence, and blocker truth. Only content depth may differ.

Failure prevented: Lite becoming an independent truth owner or silently publishing partial route families.

Archive snapshots are family-refresh snapshots. A refresh creates one timestamp folder per route/family containing the family symbols. Per-symbol timestamp scatter is not valid unless explicitly designed and justified outside current-truth ownership.

Failure prevented: archive/history folders being mistaken for current truth or Main/Lite topology parity.

Compile workflow law: operator-reported clean compile is class-2 compile status only. A MetaEditor transcript is class-4 compile evidence. If compile fails, stop on exact errors; if compile succeeds, proceed to the next bounded live proof without demanding transcript as a blocker.

Failure prevented: endless identity/report loops after the active failure has moved to refresh/runtime breathing.

## RUN255R Compile/Deploy/Output-Root Identity Law

Compile success is not deployment proof. A valid repair run must capture the intended `.mq5` path, MetaEditor output, generated `.ex5` path and modified time, active MT5 chart attachment, Experts/Journal OnInit identity line, terminal data folder, common Files output-root hint, and fresh Scanner/Market Board/Manifest/FunctionResults/heartbeat source identity. Source files may emit identity telemetry, but no route behavior, FileIO path, membership, rank/order/formula, HUD, or trade logic may be changed to solve a deployment-proof gap.

Failure prevented: source patch success being mistaken for active-terminal proof, or a stale exported folder being audited as current runtime truth.

## RUN237R Coding Brain Addition — Control Identity Is Compile-Path Context

Compile-visible run/package strings and active control headers are part of the source-control boundary. Updating them is allowed as a tiny final-gate patch when stale authority would mislabel the next compile/live proof. This must not touch FileIO, route paths, L3 gates, L4 ranking/formula, writer ownership, HUD, Dossier/Current Focus broad logic, or generated evidence.

Acceptance constraint: the next live run must compile first and report the visible run/package identity before using generated output as evidence.

## RUN235R Coding Brain Amendment — L4 Fields Must Be Additive And Source-Owned

Add L4 transparency at `ASC_ShortlistSelectionEngine` and Dispatcher commit boundaries first. Artifact writers may display committed L4 fields only; they must not compute ranking, infer missing correlation, guess exposure clusters, alter L3/L4 formulas, mutate FileIO/path topology, patch HUD, or rewrite Dossier/Current Focus. Unknown/quarantined is safer than fake precision.

Failure prevented: compile-risky hidden compute, source-of-truth drift, and fake formula/correlation/exposure proof.

## RUN231R Coding Brain Amendment — Market Board Matrix Is Display-Only

Market Board writer may add SIAM DOSSIER REQUEST BLOCK and TOP10 NUMERIC MATRIX only by consuming existing source-owned shortlist, classification, market-watch, freshness, friction, and review-state values. Missing membership snapshot id, duplicate route metadata, section coherence, correlation, or health fields must remain explicit unknowns unless the source owner exposes them. Do not calculate L3/L4 scores, infer route truth from folders, mutate FileIO/path topology, change membership/order/formula, patch HUD, or broaden Dossier/Current Focus.

Failure prevented: compile-risky hidden compute, fake matrix precision, and source-of-truth drift inside the artifact writer.

## RUN230R Coding Brain Amendment — Duplicate Resolver Must Live At The Committed Snapshot Boundary

Duplicate route resolution belongs at the source boundary where both committed membership snapshots can be read. Writers may pass through duplicate fields; composers may emit them; neither may compute membership, scan generated folders, guess from suffixes, mutate final paths, rewrite FileIO, or change L3/L4 rank/order/formula. True/false requires an available opposite snapshot; unavailable or unaligned snapshots must emit unknown with an explicit source-index missing reason.

Failure prevented: writer-owned membership collapse, stale filesystem truth, and false duplicate precision.

## RUN224R Coding Brain Amendment — Crypto Health Must Be Additive And Source-Owned

Crypto health gates belong in the existing DATA PURITY SUMMARY as additive key=value fields. Applicability must come from source-owned asset_class/bucket only. Missing inputs must be listed; unknown is safer than fake clean. Required-token validation may protect the fields, but FileIO, route paths, membership, ranking/order/formula, HUD, Market Board, and trading logic remain forbidden.

Failure prevented: broad rewrite or hidden compute turning data cleanliness into a new truth owner.

## RUN220R Coding Brain Amendment — Summary Blocks Must Copy Source Truth, Not Invent It

A DATA PURITY SUMMARY may summarize only fields already owned by active source. Unknown future hydration must remain `unknown` or explicitly false until the responsible owner exists and evidence upgrades it. Required-token validation should protect the block, but the patch must not rewrite FileIO, route paths, membership, ranking/order/formula, HUD, Market Board, or trading logic.

Failure prevented: Siam manually hunting through payloads or treating an invented summary field as proof.

## RUN217R Coding Brain Amendment — Observability Repair Without Ownership Drift

When a live blocker is missing proof visibility, patch the smallest source owner path that carries the proof. Preserve raw proof, add exact machine-readable fields, record first failure reason, and do not rewrite FileIO, path constants, membership, ranking, HUD, Market Board, Dossier, or trade logic. Compile/runtime proof remains pending until supplied.

## RUN216R Coding Brain Amendment — Data/Dossier Cleanliness Is Truth-Preserving Only

Future coding runs must include a minor DATA / DOSSIER CLEANLINESS AND TRUTHNESS CHECK. Allowed changes are bounded to truth readability: heading order, grouped proof blocks, explicit missingness, parser-safe key=value consistency, debug/body fences, and Dossier/Data Pack organization when the source owner is in scope.

Forbidden as a side job: deleting proof fields, hiding errors, mutating generated output, changing FileIO/final/staging/archive path doctrine, changing membership/ranking/formula, moving truth ownership into HUD/Market Board/Lite, broad Dossier/Composer rewrites, or adding signal/trade/risk language.

Failure prevented: making data prettier while reducing evidence quality or creating a hidden truth owner.

## RUN214R Coding Brain Amendment — Compile-Visible Identity and Checklist Lock

Before a live proof run, `ASC_Version.mqh`, office/control files, package manifest, and the run report must agree on current run, package name, and next live proof. If source-present hardening is verified but compile-visible identity still points to the previous run, patch only the identity/control layer unless a direct RUN215-blocking source defect is proven.

Converted constraint: RUN215 must verify compile output and generated output surfaces; RUN214 may not request or mutate those outputs.

## RUN213R Coding Brain Lesson — Trim Return Values and Route Reason Prefixes

MQL5 trim helpers return integer counts and mutate by reference. When compile warnings point at trim statements, use a tiny local trimmed-copy helper instead of assigning trim return values or expanding architecture. Long route reasons must preserve raw proof, but compact critical tokens should appear first to reduce truncation/readability risk.

## RUN212R Coding Brain Amendment — Control Authority Cannot Skip Source Gates

After RUN211R, compile-visible identity and active control files must point to RUN213R and RUN214R as source/control gates before RUN215R live proof. Control patches may realign run identity and package law, but they may not mutate generated runtime output, FileIO, path constants, membership, ranking/formula logic, HUD, Market Board, or strategy/trade/risk logic. RUN211 source changes must be verified before expansion.

Failure prevented: a source patch becoming a live-output retest without a source hardening gate and final checklist gate.


## RUN209R Durable Coding Lesson — Static Gate Before Runtime Proof

Before live proof, inspect active owners rather than trusting report momentum: Composer emission, Contracts field ownership, Writer reconciliation safety, Routing/path helpers, Dispatcher call boundaries, FunctionResults/Scanner Status observability, FileIO non-mutation, and required-token scope.

A safe final gate may update control/version/report authority and the next live checklist. It must not expand payloads, hard-required tokens, file paths, membership, rank/order/formula, HUD, Market Board, strategy, signal, execution, or risk logic unless a tiny RUN210-blocking defect is directly proven.

Acceptance falsifier: if a claimed field is not in both its source owner and emitted text path, or if cleanup can delete without a source-owned expected set, HOLD before live proof.

## RUN208R Durable Coding Lesson — Second-Cycle Verification Before Live Proof

After RUN207-style data-purity patches, the next source gate must inspect active owners before changing anything. Required checks: field source presence, composer emission, contract struct ownership, writer token scope, RUN206 reconciliation safety, final/staging/archive path stability, FileIO non-mutation, static compile-risk sweep, and control/version sequence alignment.

Old RUN208 compact-candle text and later RUN210-RUN213 chain text are inheritance evidence only unless current source/control explicitly re-authorizes them. They cannot be used to skip RUN208/RUN209 or to claim live proof.

Failure prevented: source/control drift where report momentum adds features, expands hard-required tokens, or advances to live proof before compile/runtime evidence exists.

# RUN194R Coding Brain Amendment — Post-Revert Baseline Lock

## RUN206R Coding Brain Addition — Destructive Reconciliation Requires Source-Owned Expected Sets

Final-folder reconciliation is destructive-risk code. It may archive/delete only unexpected child files under the intended route/bucket folder, and only when the expected set comes from the committed membership snapshot, not from final-folder contents, staging, archive, or Lite recomputation. Empty expected sets must defer cleanup and log `no_committed_membership_no_delete`; they must not mass-delete during weekend, closed-market, no-snapshot, unknown, or queue-empty states.

Required proof shape for future prompts/reports: route, bucket, expected_count, found_final_count, expected_symbols, unexpected_symbols, archived_count, deleted_count, skipped_count, first_failure_reason, final_path, archive_path, and current_truth_source.

Failure prevented: valid current children being deleted because a bucket has no active queue/membership snapshot, and stale filesystem state becoming a hidden membership source.


The reverted RUN193R source is the current authority for Symbol Data Pack child writes. RUN196R-RUN213R are failure evidence, not source authority. Do not import their token expansions, staging/archive changes, body enrichment, or compile-repair edits without a fresh owner trace from active RUN193R files.

Mandatory rule before any Lite writer/composer change:
1. Trace route membership source, dispatcher callsite, writer API, composer payload, required-token validation, FileIO atomic promotion, and final target path.
2. Check writer required tokens against the actual `ASC_SymbolDataPackComposeSkeleton()` payload for flat transition, GlobalTop10 mirror, and Top5PerBucket mirror routes.
3. Reject repo-wide token search as proof. The token must be emitted by the composed child payload that the writer validates.
4. Preserve final child writes before adding staging/archive. Staging/archive are wrappers around the current final path, not replacements.
5. Treat flat root SDP files as deprecated transition evidence and exclude them from target topology proof.

Failure prevented: RUN210-RUN213 style hard-token/staging/archive drift that created child write failure and later compile/string-literal damage.

## RUN211R Durable Lesson — Required Token Source Presence Is Not Payload Emission Proof

A required token existing somewhere in source is not enough. For Symbol Data Pack child files, the token must be emitted by the actual route-specific composed child payload returned by `ASC_SymbolDataPackComposeSkeleton()` and then validated by the writer against that payload string. Future audits must check writer required tokens against the actual compose path for flat, GlobalTop10, and Top5PerBucket routes before claiming child-write readiness.

## RUN198R Coding Brain Amendment — L4 Snapshot Proof Without Rank Mutation

1. `ASC_ShortlistSelectionEngine` remains the Layer 4 source owner for shortlist membership proof.
2. Adding bounded proof tokens is allowed only when it does not change rank formula, score formula, GlobalTop10 membership formula, Top5PerBucket membership formula, cadence, route ownership, writer ownership, FileIO, HUD, Market Board logic, or strategy/execution behavior.
3. Dossier and Symbol Data Pack consumers may read `l4_proof_tokens`; they may not recompute membership or create alternate snapshot IDs.
4. `l4_source_order_changed_by_lite=false` and `l4_rank_order_formula_changed=false` are required guard tokens whenever Lite mirrors L4 membership proof.

Failure prevented: source-contract drift where proof fields become a second ranking engine.

# RUN197R Coding Brain Amendment — Layer 3 Candidate Contract Fields

Durable coding law added by RUN197R:
1. `ASC_SelectionFilter` is the active Layer 3 candidate admissibility owner unless a newer source patch supersedes it.
2. Layer 3 must expose bounded machine-readable fields for analytic availability, support completeness, shortlist admissibility, carry-only state, placeholder-heavy state, primary/secondary blockers, blocker count, source posture, freshness, dependency state, reason-if-not-admissible, and L3→L4 handoff.
3. Layer 3 may expose candidate support and handoff permission, but must not mutate Layer 4 rank/order/formula or create a new selection/ranking/calculation owner.
4. Placeholder-heavy, stale, partial, source-unavailable, or carried truth must be visible as such and must not be silently projected as clean shortlist authority.
5. RUN198R may consume these fields in Layer 4, but any rank/order/formula change still requires separate authorization.

Failure prevented: downstream consumers reading a legacy pass/pending token as clean L4 authority without knowing source age, placeholder/carry state, or blocker reason.

---

# RUN196R Coding Brain Amendment — Mandatory No-Drift Gates

Durable coding law restored by RUN196R:
1. Every serious run must show the two-brain check. It is not optional, hidden, or conditional.
2. Every serious run must show an official/external research ledger when MQL5 runtime/file/data behavior, process rules, or trading-data readiness constrains the patch.
3. Active source files outrank reports, memory, screenshots, archive evidence, and git history.
4. Active compile-visible control such as `ASC_Version.mqh` must agree with active roadmap/control files before any live proof run is authorized.
5. No source patch is allowed without inspecting the source owner and likely variants.
6. Changed-files packages must preserve `Aurora Sentinel Core/...` layout and must not be empty, rootless, or whole-repo dumps.
7. Meaningful source/control patches require version/control/office/manifest updates.
8. Do not rewrite FileIO unless the exact blocker proves FileIO itself is the owner.
9. Do not patch HUD, Market Board logic, GlobalTop10/Top5PerBucket rank/order/formula, candidate score, heartbeat cadence, strategy, or execution from a control-reset run.
10. Do not allow a report-only loop when an active, safe, patchable source/control gap exists.
11. Do not claim compile, runtime, output, live, or production proof above the available evidence rank.

Failure prevented: RUN196 specifically blocks the drift where RUN195 source patch momentum incorrectly pointed the next live checkpoint at RUN196 instead of the operator-locked RUN210R.

---

# RUN193R Durable Coding Lesson — Root Family Output Separation

Direct root symbol support files (`Dossiers/<symbol>.txt`) are compatibility-only by default. New or repaired publication paths must not treat root symbol files as the same owner as:
- Current Focus (`Dossier.txt`);
- `Dossiers/GlobalTop10/<symbol>.txt`;
- `Dossiers/Top5PerBucket/<bucket>/<symbol>.txt`;
- mirrored Symbol Data Pack family outputs.

If a future patch needs root direct symbol output, it must explicitly enable a compatibility flag, state why Current Focus or family children are insufficient, and prove that it will not restart full-universe root spam.

---

# RUN190R CODING BRAIN AMENDMENT — ROUTE HELPERS ARE NOT PUBLICATION PROOF

```text
GUIDEBOOK RULE ADDED BY RUN190R
DECISION: TEST FIRST
```

RUN190R helper existence proves only source-level route readiness. It does not prove mirrored Symbol Data Pack output.

Future RUN191R/RUN192R coding rules:
1. Use `ASC_SymbolDataPackTargetPathForRoute(...)` instead of inventing new path strings.
2. Use `ASC_SDP_FAMILY_ROUTE_GLOBAL_TOP10` for GlobalTop10 SDP children.
3. Use `ASC_SDP_FAMILY_ROUTE_TOP5_PER_BUCKET` and a sanitized bucket token for Top5PerBucket SDP children.
4. Use existing FileIO atomic publication helpers; do not rewrite FileIO.
5. Use existing Dossier/top-list membership authority only; do not recompute rank/order/formula.
6. Do not treat flat root SDP output as target topology proof.
7. Do not add HUD or Market Board backfill ownership.
8. Live proof waits until RUN195 after RUN191–RUN194 complete.

---
# RUN189R GUIDEBOOK LOCK — SYMBOL DATA PACK FAMILY TOPOLOGY

```text
GUIDEBOOK RULE ADDED BY RUN189R
DECISION: TEST FIRST
```

When patching Symbol Data Packs during RUN190R–RUN194R:

1. Mirror existing `Dossiers/GlobalTop10` and `Dossiers/Top5PerBucket` membership.
2. Do not compute or mutate top-list rank, order, bucket, score, or formula.
3. Do not rewrite `ASC_FileIO.mqh`.
4. Do not change heartbeat cadence to hide write pressure.
5. Treat flat `Symbol Data Packs/<symbol>_SYMBOL_DATA_PACK.txt` as deprecated transition evidence, not topology proof.
6. Suppress direct root Dossier symbol spam only after mirror readiness is present and selected/current dossier semantics remain intact.
7. Keep Lite content Dossier-like but lighter: factual L0–L5 only, no Layer 6, no heavy deep-only narrative, no strategy/execution claims.
8. RUN195 proof must include recurrence after next_due, mirrored family files, manifests, Scanner Status, and root-spam suppression evidence.

---

# RUN186R CODING BRAIN AMENDMENT — PROOF-SURFACE FIELD ALIGNMENT

When a runtime proof line emits a decision-critical field, the corresponding artifact required-token contract must hard-require it. For Lite proof surfaces, `activation_enabled`, `next_due_utc`, `fresh_write_success`, `retained_last_good`, `continuity_state`, `writer_reason`, `fileio_missing_token_propagation`, and `publication_health` are not optional decoration; they prevent fake success and must survive validation.

Do not solve proof-surface gaps by weakening validation or rewriting FileIO. Patch the proof contract at the surface that emits the field, then compile before live output proof.

---

## RUN185R Durable Coding Lesson — Emitted Proof Fields Must Be Hard-Required When They Gate Live Readiness

A proof field that appears in Scanner Status or Artifact Bundle Manifest but is not part of that artifact's required-token contract can silently disappear later without failing validation. For live-readiness proof surfaces, emitted fields that decide write success, no-change, retained-last-good continuity, validation failure, writer state/reason, final/temp/last-good paths, and last error must be both emitted and hard-required where that artifact is used as proof.

RUN185R applies this only to Lite proof-surface required-token alignment. It does not weaken validation and does not change FileIO, HUD, Market Board, rank/order/formula, heartbeat, strategy, execution, Dossier, or Current Focus behavior.

---

## RUN184R Durable Coding Lesson — Proof Parsers Must Match Writer Contracts

A proof-surface parser is part of the publication contract. If a writer can emit `missing_required_token=` or lower-level FileIO can emit `missing_token=`, status extraction must recognize both. Do not truncate required tokens at spaces because required tokens may be human-readable headings such as `Publication / Atomic Status`.

Durable rules:
- Required-token diagnostics must preserve the exact missing token when possible.
- No-change and retained-last-good continuity are not the same as fresh file publication.
- Retained-last-good continuity may protect operators from data loss, but it must not be counted as fresh output proof.
- Payload path proof must use the same symbol-to-file token rules as the writer.
- Repair proof visibility before widening runtime behavior.

---

## RUN179R Durable Coding Lesson — Protected-Area Repair Contracts Before Patching

Dispatcher/FileIO exceptions, HUD purity, Market Board ownership, and Top5PerBucket/GlobalTop10 protection are protected areas. A future coding run must first state current evidence, allowed inspection, allowed patch type, forbidden patch type, acceptance criteria, falsifier, earliest run, and kill condition. Do not replace FileDelete, split Dispatcher, repair HUD, mutate Market Board logic, or touch rank/order/formula until the contract authorizes the exact file and test.

---

## RUN177R Durable Lesson — Compile-Visible Identity Must Match Active Control

RUN177R closes the authority gap left by RUN176R: a cancelled live chain is still dangerous if `ASC_Version.mqh` exposes the stale current run or next-live target. Future runs must treat compile-visible version/status strings as active authority when they can appear in runtime/status artifacts.

Durable rules:
- A cancelled live run must be removed from active control and compile-visible identity before any future live work.
- Source-patch success is not compile proof, live proof, runtime proof, or output proof.
- Historical report text may remain below newer authority, but it must not be used as the active next-run seed when a newer control section supersedes it.
- Root office authority must either exist as current minimal files or be explicitly delegated to the Truth Seeker roadmap files; ambiguity is a HOLD condition.

---

## RUN176R Durable Lesson — Truth Furnace Audit Gate

RUN176R cancels stale live-chain inertia. Future run planning must separate source patch success from compile proof, runtime proof, live output proof, and roadmap authority proof. A live run is forbidden while the active roadmap, manifest, guidebooks, and compile-visible version identity disagree about the current run or next-live marker.

Durable rule:
- Do not schedule a live proof run from report momentum alone.
- Do not treat `PASS-BY-SOURCE-PATCH` or `PASS-WITHOUT-SOURCE-PATCH` as system proof.
- Mark assumptions explicitly.
- Any next-live marker must have a current control-file owner, a source/version identity owner, capture requirements, and kill conditions.


---

## RUN168R Coding Lesson — Health Aggregation Without Runtime Expansion

A health map is an aggregation surface, not a new source owner. It may summarize source metadata, requested/received counts, bars required/available, buffer loading, history synchronization, retry state, and last-good state, but it must not trigger broad recalculation, create indicator handles, rewrite FileIO, activate dispatcher callsites, or create per-health/per-section files. Missing timestamps and owner-pending states must remain visible instead of becoming lazy unavailable defaults.

# AURORA CODING BRAIN GUIDEBOOK

## Authority Status

RUN166B repairs a guidebook-authority gap. The RUN166R guidebook file was a compressed replacement created because the prior guidebook tree was absent from the RUN166R intake. This file now preserves the RUN166R durable lesson while reconstructing the accumulated Lite Pack rules from RUN165R/RUN166R reports and active source contracts.

## Source Patch Discipline

| Rule | Constraint | Validation |
|---|---|---|
| Patch real files only | edit active `Aurora Sentinel Core/...` paths only | changed-files zip preserves root layout |
| No authority truncation | guidebooks/control files must be merged, not replaced by compressed fragments | compare file size, headings, prior run continuity |
| Source before report | inspect source contracts before claiming source safety | grep and static sweep |
| No runtime activation from dormant runs | Lite source patches may be compile-visible without enabling live writes | dispatcher/writer callsite sweep |
| No duplicate implementation | extend existing Symbol Data Pack helpers instead of creating a second VWAP path | duplicate constant/helper sweep |
| Atomic writer preservation | do not rewrite FileIO or one-file-per-symbol publication law | FileIO unchanged sweep |
| Package validation | every final zip must be created and extraction-tested before link handoff | zip checklist in report |

## RUN162R-RUN166R Factual Pack Contracts

- RUN162R: one-file-per-symbol writer foundation; token validation; no unchanged-file churn.
- RUN163R: quote/tick/spread sections with freshness, retry, hydration, and last-good states.
- RUN164R: OHLC/high-low sections with CopyRates evidence and current/closed bar separation.
- RUN165R: ATR/range/pullback sections with zero guards and due-only recalculation.
- RUN166R: VWAP/anchored VWAP/volume-source quality sections with explicit formula basis, M1 CopyRates/MqlRates source, sample counts, partial coverage, and zero-volume guards.

## RUN166B Coding Lesson

Changed-files patches must include a guidebook/control authority check. If a package contains compressed guidebook/control files but prior reports prove accumulated authority existed, the next repair run must restore/merge from valid authority and must not advance the roadmap until truncation risk is closed.

## VWAP Implementation Discipline

- Formula basis must be explicit.
- Typical price method must be explicit.
- Volume source must distinguish `real_volume` from `tick_volume_proxy`.
- Zero-volume windows must fail closed into hydration/retry state.
- Partial coverage must be visible.
- Last-good age must be visible.
- Recalculation must remain due-only and bounded.
- No per-anchor files and no per-timeframe operator files.
- No change to Dossier, Market Board, Top5PerBucket, GlobalTop10, FileIO, heartbeat, or HUD.

## RUN167R Coding Guard

Numeric indicator facts may be added only as factual values with formula/source/coverage/freshness fields. RUN167R must not add interpretation labels, runtime write activation, new output families, or formula/rank/order changes outside the Lite Pack source area.

## RUN167R Coding Lesson

Official MQL5 indicator handles may be used in the Lite Pack only when the payload exposes the handle family, buffer/source method, timeframe, applied price, bar index, sample count, hydration state, retry state, and last-good age. Runtime activation still requires handle caching by symbol/timeframe/family/parameters and release on deinit or symbol removal; the current source patch remains shadow/dormant and releases handles after bounded compose attempts.

Compile-risk guard: CopyBuffer sections must expose values requested/received, BarsCalculated state, buffer indexes, divide-by-zero guards for band position and z-score, and writer required-token validation so missing indicator sections cannot masquerade as complete output.

## RUN173R Coding Lesson — Writer Token Validation Must Match Composer Payload

A live writer failure caused by `payload_guard_failed` and `missing_required_token=...` is a source-real, patchable proof-surface bug when the missing token is already part of the approved contract vocabulary but is not emitted by the composer payload. Repair must add the exact required token to the bounded composer/contract text, not loosen validation, not bypass required-token checks, not rewrite FileIO, and not downgrade the live failure into a fake pass. The re-run must prove final/temp/last-good/no-change behavior from runtime output before PASS.
## RUN174R Coding Lesson — Validator/Composer/Proof-Surface Alignment

Required-token validation is only useful if three surfaces stay aligned: the writer's exact required token list, the composer payload that must emit those exact tokens before validation, and the runtime/status/manifest proof fields that expose the first failed token without contradiction. A payload guard failure must not report `missing_token_count=0` or `first_missing_token=none` when the writer reason contains `missing_required_token=...`.

Durable rule: patch the smallest diagnostic/proof-surface gap first; do not weaken validation and do not rewrite FileIO, heartbeat cadence, ranking, HUD, or trading logic to make a token guard pass.


## RUN191R Coding Lesson — Family Mirrors Consume Existing Membership Only

A Symbol Data Pack family mirror may publish children for GlobalTop10 only from an already committed membership/order snapshot. It must never sort, filter, re-rank, recompute Market Board output, or use HUD state as hidden truth. The mirror callsite must be bounded, due-aware, route-aware, and must expose final/temp/last-good/status proof separately from the flat transition route. If the snapshot is absent, the correct outcome is HOLD/deferred status, not fake publication.


## RUN192R Coding Lesson — Top5PerBucket Mirrors Consume the Existing Bucket Queue

A Top5PerBucket Symbol Data Pack mirror may publish bucket-child files only from the existing Top5PerBucket Dossier queue authority: symbols, slugs, display names, ranks, and queue signature. It must not recompute membership, sort, filter, re-rank, consult HUD state, or use Market Board output as a hidden truth owner. The mirror callsite must be bounded, due-aware, bucket-token validated, and must expose route, bucket token/name, final/temp/last-good paths, no-change, retained-last-good, missing-token, and live-proof-false status separately from the flat transition and GlobalTop10 routes.


## RUN201R Coding Brain Amendment — SDP Clean Body Split
1. Symbol Data Pack Lite payloads must keep `DEBUG_HEADER_BEGIN` / `DEBUG_HEADER_END`, `SIAM_DATA_BODY_BEGIN` / `SIAM_DATA_BODY_END`, `MEMBERSHIP_PROOF_BEGIN` / `MEMBERSHIP_PROOF_END`, `FAMILY_PROOF_BEGIN` / `FAMILY_PROOF_END`, and marker-only data-health boundaries stable.
2. Moving existing scaffolding into bounded markers is allowed only when it does not add hydration, routes, membership ownership, ranking, FileIO behavior, dispatcher behavior, Market Board logic, HUD behavior, heartbeat cadence, strategy, or execution behavior.
3. Required-token validation must protect the section markers so future payload drift is caught by source-level contract checks.
4. Debug/source-contract text may remain visible for audit, but it must not pollute the Siam-facing factual body.

Failure prevented: clean-body drift where a Lite file becomes hard to parse, debug-heavy, or accidentally treated as a second source of trading truth.

## RUN202R Coding Brain Addition — Data-Health Semantic Labels

1. Any sample-window min/max/avg field must check sample count before printing a number. If sample count is zero, print `no_sample` and an invalid-reason field.
2. Valid zero spread requires current tick success, `bid > 0`, `ask > 0`, `bid == ask`, fresh tick age, and synchronized symbol state. Missing bid/ask, stale tick, failed tick read, or unsynchronized symbol must use bad-zero/no-current-tick labels.
3. `CopyTicks` and `CopyRates` wrappers must expose requested count, returned/copied count, error code, partial/cap-hit/readiness state, and section timestamps where the body compares sections.
4. `last=0` must be labelled unavailable/broker-not-supplying unless a valid zero source is proven.

Failure prevented: false numeric calm, fake zero spread, hidden partial history, and compile-visible body tokens that imply complete data without source proof.

## RUN203R Coding Brain Lesson — Bounded Verdicts Must Stay Consumer-Side

Siam readiness verdict fields may summarize existing quote, tick, spread, OHLC, membership, family, body, source-identity, and asset-class labels, but may not hydrate new specs/session/indicator data, recalculate membership, mutate rank/order/formula, create routes, or become a hidden truth owner. Writer validation must include the new readiness tokens so atomic publication rejects partial verdict bodies.

Failure prevented: a readiness patch silently becoming a hydration, ranking, or execution patch.


## RUN204R Durable Lesson — Symbol Specs Are Factual Context Only

Symbol specification hydration may read broker properties through `SymbolInfoInteger`, `SymbolInfoDouble`, and `SymbolInfoString` bool overloads, but every emitted field must carry source-status/error context when unavailable. Digits, point, tick size/value, contract size, volume rules, currencies, and trade/execution/order modes are broker facts only. They must never become risk sizing, lot recommendation, trade permission, signal permission, execution authority, rank/order/formula input, or a new selection owner.

## RUN205R Durable Lesson — Bounded Session Hydration

`SymbolInfoSessionQuote` and `SymbolInfoSessionTrade` reads must be bounded by a small scan cap, expose scan completion/truncation, and record source status/error context. A failed session call must not be converted into `closed`; it is unavailable until a bounded successful source proves a session window. Writer validation must require market/session markers and permission guards.

Failure prevented: unbounded session loops, timer/write pressure, fake open/closed states, and partial session payloads passing validation.


## RUN206R Durable Coding Guard — ATR / Range Normalization

ATR, spread-to-ATR, and range-to-ATR fields are data-quality normalization only. Invalid denominators, no-sample spread, unavailable ATR, or partial CopyRates history must emit explicit `not_calculable`, `not_ready`, `partial`, or error-state labels. Do not convert ATR/range fields into ranking, route ownership, risk sizing, stop distance, entry, execution, or trade/signal permission.

## RUN208R Durable Coding Lesson — Compact Candle Facts Are Bounded Data Product Fields

Compact candle sequences may be added only as a bounded closed-bar factual helper inside the existing Symbol Data Pack source lane. The helper must expose CopyRates/MqlRates requested/copied/partial/error/synchronization state, closed-bar policy, emitted count, and not-ready labels. It must not become a route, ranking, selection, formula, strategy, risk, HUD, Market Board, FileIO, or execution owner.

Failure prevented: a recent-candle section silently becoming an unbounded archive or hidden interpretation engine.


## RUN210R Durable Coding Lesson — Writer/Composer Token Parity Must Use Actual Route Payloads

Writer required tokens must be parity-tested against the actual route-specific composed payload path, not merely searched in source files or reports. For Symbol Data Pack Lite, flat transition, GlobalTop10 mirror, and Top5PerBucket mirror children must each prove that the writer-required tokens are emitted by the child payload that will be staged, read back, validated, archived if needed, and promoted. Staging and archive are publication lifecycle proof surfaces only; they must not become membership, ranking, strategy, execution, or current-truth authorities.

Failure prevented: folders/manifests appearing healthy while child files silently fail on a missing required token such as `Symbol Data Pack Lite Health Map`.

RUN213R compile-repair lesson: Long token/health-map text in MQL5 must be emitted through safe escaped line appends; a required token existing in source is useless if the source does not compile.

## RUN198R Source Verification / RUN199 Live Contract Lock

- Final SDP child files are current truth; staging files are candidate/proof only.
- Archive files are one-deep previous-final continuity evidence only, never current truth.
- Staging/archive success must never be counted as final publish success.
- Writer hard-required tokens must not expand unless the active composer emits the token on every active route.
- Lite mirrors committed Main/L4 membership only; it does not own rank, formula, or membership.
- RUN199 live proof is mandatory before any readiness claim.

## RUN200R Durable Coding Lesson — Lifecycle Logs Must Prove State Without Becoming Owners
- Scanner Status required-token parity must be checked against the actual emitted payload path before live proof.
- Main Dossier lifecycle is the comparison standard for publication flow, but Lite SDP may differ intentionally in payload richness because it has no signal/trade data.
- Lifecycle diagnostics must expose due, attempt, route, membership source, staging, archive, final publish, no-change, retained-last-good, first failure, duration/write-pressure, current truth path, and next_due.
- Logging must stay bounded: one concise key=value lifecycle line per route/symbol attempt or lifecycle decision; no FileIO rewrite, no flush loop, no final path mutation, no rank/order/formula change, and no hard-required writer token expansion unless the active composer emits the token on every route.

## RUN207R Coding Brain Addition — Data-Purity Semantics Are Source-Owned and Non-Destructive

Siam data-purity labels may be patched only in the source owner that emits the field. Missing tick/spread samples must be labelled with `no_sample` plus validity/invalid-reason fields rather than measured-looking numeric zeros. Broker zero-spread and broker zero-last-price conditions must preserve raw values but expose availability/status labels. CopyTicks windows must expose requested limit, returned count, cap-hit/truncation state, and error code when those values are in local scope.

Forbidden in the same patch: FileIO rewrite, final/staging/archive path changes, writer required-token expansion, membership/ranking changes, broad payload enrichment, HUD/Market Board changes, and signal/execution language.

Failure prevented: replacing ambiguous data with fake precision, or letting a small semantic repair drift into publication-path or selection ownership changes.

## RUN236R Coding Brain Addition — Projection Contracts Must Thread Through Existing Owners

When a sync field is required across Board, SDP, Scanner Status, or FunctionResults, first locate the existing committed source owner. Then thread the field through existing call paths as compact metadata. Do not widen writer responsibility, scan generated folders, mutate final/staging/archive routes, or expand required tokens unless the active composer emits them on every active route.

Failure prevented: compile-safe-looking payload enrichment that breaks required-token parity, creates writer-owned truth, or hides a split snapshot chain.


## RUN260R Coding Brain Patch — Internet Research Must Become Required-Token Tests

Runtime/proof/file/timer/lineage/retry/backoff patch runs must perform external research before patching and convert it into enforceable code constraints. For required-token validators, the active required-token array may contain only tokens emitted by the same body/write path, and old future-run or legacy-live-contract tokens may remain only as non-required historical context. Missing-token telemetry must expose the exact missing token; degraded truthful output is safer than stale silence or fake success.

Acceptance falsifier: if any active required token contains stale RUN160/RUN253-era future-live law, is not emitted by the current body path, or validation is weakened into an empty/no-op pass, HOLD/KILL before Main/Lite or live proof.

## RUN261R Coding Brain Patch — Main/Lite Mirror Contract

Dossiers are Main. Symbol Data Packs are Lite. Lite may be lighter in content depth only; it must mirror Main route family layout, bucket folders, symbol set, rank/order, snapshot/epoch lineage, and blocker/degraded truth from the source-owned Main family plan. SDP writers/composers may project `main_lite_contract_version`, slave status, source snapshot, publication epoch, expected/written counts, rank/order parity, and blocker state, but must not compute membership, infer from current/final/staging/archive folders, mutate FileIO/final paths, alter L3/L4 ranking, or patch HUD/trading logic.

Failure prevented: Lite appearing partially healthy while silently missing Main symbols or buckets, or archive/folder state being mistaken for current source truth.

## RUN265R Coding Brain Amendment — Scheduler Guards Before Heavy Payloads

For heartbeat breathing repairs, patch the earliest scheduler owner only. Heavy lanes must call preflight before string/payload construction or filesystem-heavy work. Side-duty must expose `side_duty_budget_guard`, `side_duty_skipped_before_work`, and `side_duty_skip_reason`; presence sweep must expose due/backoff/skip/next_due telemetry.

Do not solve timer pressure by rewriting FileIO, cleaning generated output, changing final paths, mutating GlobalTop10/Top5 membership/ranking, touching Main/Lite/archive topology, or hiding side-duty. Degraded/skipped is valid only when source-owned telemetry says exactly why.

Acceptance constraint: static source must show prebuild guards for deep/top5 heavy paths, presence-sweep due/backoff/cooldown state in runtime state, and heartbeat telemetry fields that make skipped work traceable.

## RUN266R Coding Brain Amendment — Required-Token Body Parity + Admission Gates

For Manifest/publication-chain runs, the earliest source owner is the writer body plus its required-token validator. Do not patch downstream route families until Manifest validation can truthfully pass or expose the exact missing token. Required-token arrays must not contain stale renamed labels such as old dirty-loop wording when the current contract has already removed that label.

Publication admission telemetry must be source-owned and bounded: `route_family`, `admission_status`, `admission_blocker_owner`, `admission_blocker_reason`, `expected_child_count`, `written_child_count`, `source_snapshot_id`, `publication_epoch`, `retry_due`, and `fake_complete=false`. This is diagnostic admission truth, not a broad route rewrite.

Acceptance falsifier: if Manifest can fail on a required token that the Manifest body cannot emit, or if GlobalTop10/Top5/SDP remain silently absent without blocker ownership, HOLD before Main/Lite, archive, HUD, ranking, or live proof.


## RUN267R — GlobalTop10/Top5/SDP Route Admission Counter Repair

- Selected uploaded RUN266R package as baseline after zip-first inspection, external research, and Git-last access attempt.
- Repaired `ASC_WriteArtifactBundleManifest` route admission so SDP GlobalTop10 and SDP Top5 consume actual route counters instead of hardcoding `written_child_count=0`.
- Added `route_admission_status`, source/membership truth locks, retry due, blocker owner/reason, actual/no-change/retained/failed split visibility, and preserved `fake_complete=false`.
- Preserved RUN266 Manifest parity and RUN265 scheduler route service calls.
- No FileIO, final paths, generated output, Main/Lite broad architecture, archive topology, L3/L4, HUD, strategy, signal, trade, execution, or risk logic changed.
- Decision: TEST FIRST; compile and bounded runtime readback required.


## RUN268R Brain Guidebook / Two-Brain Check — Final Source Gate

RUN268R law: before RUN269 live proof, the source gate must verify the repaired corridor without turning the audit into a rewrite. Research must become acceptance tests and falsifiers. Git/app access must happen last. If Git is unavailable or behind, uploaded package source remains the inspected baseline and Git drift is declared as debt, not silently resolved. RUN269 must collect live evidence for heartbeat health, Manifest publication, GlobalTop10/Top5/SDP route admission, Main/Lite parity, archive topology, previous_state_cache separation, and no-trade permission=false boundaries.

Forbidden drift: FileIO/final path mutation, generated evidence editing/deletion, HUD/L3/L4/trade/risk changes, Lite independent truth, archive/folder/staging as membership or ranking source, or fake complete route claims.


## RUN271R Coding Brain Addition — Cheap Board-Child Safety Gate

Board↔Dossier↔SDP safety must be enforced with cheap source-owned signatures, not child-folder scans, retries, or FileIO rewrites. If committed child-route signature proof does not match the current Board snapshot, emit `board_authority=degraded`, `board_child_sync_blocking=true`, request count `0`, a blocker reason, and false permission fields.

Forbidden in this gate: L3/L4 formula/rank/order/membership changes, Top5 Main/Lite parity repair, generated output edits, FileIO/final-path mutation, archive/cache/staging truth ownership, HUD hidden truth, and strategy/signal/risk changes.

Failure prevented: retry storms and source-of-truth drift while trying to make the Board safer.

## RUN282R Coding Brain Addition — Failed Live Proof Exact-Owner Repair

Failed live proof defects must be repaired at the exact emitting/validating owner, not by deleting generated output, weakening FileIO, or rewriting route architecture. A degraded/provisional Market Board may publish only when it emits explicit withheld/not-actionable best-pick tokens, request count `0`, and false permission fields. Scanner Status required-token arrays must match fields the body actually emits.

Lite current truth must mirror the Main committed GlobalTop10/Top5 plan; stale final children may be reconciled only by the existing bounded route-prune owner with count/error telemetry. Operator-facing score components must not use bare `entry=`; use `entry_score=` or a clearly non-trade score label.

Failure prevented: publication failure from stale token contracts, split Main/Lite current truth, ambiguous trade-entry wording, and hidden cleanup drift after a failed live proof.
