
# ARL-RUN007 — Pre-Work Unification + Source-Record-Blueprint-MT5 Alignment Plan

Mode: AUDIT + RESEARCH + DEBUG + PATCH.

Objective: align the uploaded Work Area package, Git comparison state, office records, roadmap protocol, blueprint output law, and MT5 identity before real behavior implementation begins.

## Scope

Patch Work Area only.

Allowed:
- record alignment;
- version identity alignment;
- Git summary/description standard;
- RUN006 trader-data reference integration;
- acceptance tests;
- report/package creation;
- compile-safe MT5 identity/header fixes.

Forbidden:
- HUD;
- trading;
- signals;
- execution;
- strategy formulas;
- direct ASC archive migration;
- duplicate owner modules;
- changelog `.mqh` source.

## Evidence Order

1. Uploaded Work Area zip.
2. Git current Work Area state as comparison/check.
3. RUN006 reference files if present.
4. Archives as Git-only inheritance evidence.
5. Prior assistant memory/reports.

## Version Decision

Product version remains `1.004` unless active MT5 runtime behavior changes.
RUN007 may update source run identity to `ARL-RUN007` without version bump because this is pre-work source-record alignment only.

## Acceptance

- Work Area root confirmed.
- Archives excluded from package.
- RUN006 references present/absence logged.
- Version identity aligned.
- Static include scan passes.
- Duplicate-function scan passes.
- Compile only claimed with MetaEditor output.
- RUN007 report exists.
